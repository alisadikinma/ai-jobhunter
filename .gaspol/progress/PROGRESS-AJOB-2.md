# PROGRESS — AJOB-2: ATS-friendly DOCX rendering

**Ticket:** AJOB-2
**Plan:** docs/plans/2026-09-19-AJOB-2-ats-docx-rendering-plan.md
**Spec:** docs/plans/2026-09-19-AJOB-2-ats-docx-rendering-spec.md

## Pre-flight

| Gate | Result |
|---|---|
| Git clean | PASS — baseline `263d58d`, 0 berkas belum ter-commit |
| Baseline suite | PASS — `python3 -m unittest discover -s tests -t .`, 262 lulus / 0 gagal |
| detect-stack | nol baris (tidak ada penanda stack) — perintah dari plan: static `python3 -m compileall -q scripts tests`, unit `python3 -m unittest discover -s tests -t .` |
| Feasibility | PASS — penulis OOXML stdlib dibuktikan 2026-09-19: 1.749 byte, zip valid, terekstrak dengan struktur heading utuh |

## Keputusan saat jalan

- 2026-09-19, Ali: **DOCX saja, tanpa PDF.** Diukur: stdlib tak punya PDF writer, pandoc gagal tanpa `pdflatex`, cupsfilter cuma cetak sumber markdown. PDF jadi langkah manual Ali dari Word.
- 2026-09-19, Ali: renderer **menolak dan tidak menulis file** kalau ketemu `[verifikasi]` / `[Assumption]`, bukan sekadar memperingatkan.
- 2026-09-19, Ali: tabel dan gambar **diratakan jadi teks**, bukan ditolak — mesin bisa beres sendiri, tak perlu Ali bolak-balik.
- 2026-09-19, Ali: subcommand CLI `render-docx` yang dipanggil `tailor`, bukan skill ketujuh.
- 2026-09-19, Claude: gaya **tidak bisa dikonfigurasi** (Calibri 11pt, satu kolom, margin 2cm). Setiap opsi gaya adalah cara baru menghasilkan CV yang gagal parsing.

## Checklist

### [x] Phase A: markdown → blocks
- [x] Write failing test for `docx.parse_blocks("# Ali\n\nHello\n")` returning `[{"kind": "heading", "level": 1, "text": "Ali"}, {"kind": "paragraph", "text": "Hello"}]`. Expected error: `ModuleNotFoundError: No module named 'docx'`
- [x] Run `python3 -m unittest discover -s tests -t .`, confirm it fails for that reason
- [x] Implement `parse_blocks(markdown)` per the Block contract above: `#`/`##`/`###` → heading with level; `-` or `*` at the start of a line, leading whitespace allowed → bullet (diubah oleh Amendment 2); a blank-line-separated run of text → paragraph. Inline `**bold**`, `*italic*` and `` `code` `` markers are stripped from the text (Word carries the style, not the asterisks)
- [x] Add tests for the enumerated edge cases: empty string, whitespace-only, a heading with no text (`##` alone), `####` (level 4 — treated as a paragraph, not a heading, because the style table stops at 3), a bullet with no text, CRLF line endings, a line that is only `---`, two blank lines between paragraphs, 500 blocks, and text containing `&`, `<`, `>`
- [x] Run tests, confirm all pass
- [x] Commit: "feat(docx): parse the supported markdown subset into blocks"
- [x] `python3 -m compileall -q scripts tests` passes
- [x] `python3 -m unittest discover -s tests -t .` passes
- [x] `parse_blocks` emits only `heading`/`paragraph`/`bullet`, proven by a test that asserts the set of `kind` values over a real fixture
- [x] `####` yields a paragraph, not a level-4 heading
- [x] No placeholder/TODO comments in new code
- [x] detect-stack: no stack markers for this project — verification is plan-declared only

### [x] Phase B: the unverified-claim gate
- [x] Write failing test for `docx.ats_lint("- revenue up 40% [verifikasi]\n")` returning one finding with `reason == "unverified-claim"` and `line == 1`. Expected error: `AttributeError: module 'docx' has no attribute 'ats_lint'`
- [x] Run tests, confirm it fails for that reason
- [x] Implement `ats_lint(markdown)` returning findings per the Lint finding contract. Detect `[verifikasi]` and `[Assumption]` **case-insensitively**, anywhere on the line. Also detect and report (without refusing) `table`, `image`, `deep-nesting`, `html`
- [x] Implement `UnverifiedClaimError(Exception)` carrying `.findings`, with a message naming the file, the line number and the offending text, e.g. `cv.md:14 — "increased revenue 40% [verifikasi]"`
- [x] Add tests for the enumerated edge cases: marker in a heading, marker inside a fenced code block (**still refuses** — a CV has no reason to carry code fences, and a marker there is far more likely a real claim than a deliberate literal), `[VERIFIKASI]` uppercase, `[ Assumption ]` with inner spaces (**a match** sejak Amendment 10; dulu **not** a match — the convention is exact, and loosening it invites false positives), two markers on one line (one finding, not two), a marker on the last line with no trailing newline, and markdown with no markers at all (empty finding list)
- [x] Run tests, confirm all pass
- [x] Commit: "feat(docx): refuse to render a claim the candidate never verified"
- [x] `python3 -m compileall -q scripts tests` passes
- [x] `python3 -m unittest discover -s tests -t .` passes
- [x] A markdown file carrying `[verifikasi]` produces a finding whose `line` matches the real 1-based line number, proven against a multi-line fixture
- [x] `[ Assumption ]` with inner spaces produces no finding
- [x] Mutation check: delete the `[Assumption]` half of the pattern and confirm a test fails — a guard that guards only half of what it claims is this repository's documented recurring defect
- [x] No placeholder/TODO comments in new code
- [x] detect-stack: no stack markers for this project — verification is plan-declared only

### [x] Phase C: ATS flattening
- [x] Write failing test for `docx.flatten("| Skill | Years |\n|---|---|\n| Python | 8 |\n")` returning markdown with no `|` characters and a line reading `- Skill: Python — Years: 8` (diubah oleh Amendment 1). Expected error: `AttributeError: module 'docx' has no attribute 'flatten'`
- [x] Run tests, confirm it fails for that reason
- [x] Implement `flatten(markdown)` returning `(flattened_markdown, notes)` where `notes` is a list of strings for stderr. Transformations, all of them from spec §4 gate 2:
- [x] - **table** → one bullet per body row, tiap sel bawa header sendiri, `"- <header1>: <cell1> — <header2>: <cell2>"` (diubah oleh Amendment 1), the separator row dropped
- [x] - **image** `![alt](src)` → removed entirely; note records the `src`
- [x] - **link** `[text](url)` → `text (url)`, because ATS frequently keep neither the anchor nor the href
- [x] - **nesting deeper than one level** → flattened to one level
- [x] - **inline HTML** → stripped via `ats._clean_description`, imported rather than reimplemented
- [x] Add tests for the enumerated edge cases: a table with one column, a table with a missing cell (pad with empty string, never drop the row), a table with no header separator (treated as paragraphs, not a table), an image with no alt text, a link whose text equals its url (emit the text once, not `url (url)`), a reference-style link `[text][ref]` (left as-is — out of scope, noted), three-level nesting, an empty table, and markdown containing a literal `|` inside a normal sentence (must NOT be treated as a table)
- [x] Run tests, confirm all pass
- [x] Commit: "feat(docx): flatten what an ATS parses badly, refuse nothing"
- [x] `python3 -m compileall -q scripts tests` passes
- [x] `python3 -m unittest discover -s tests -t .` passes
- [x] `flatten` output, fed to `parse_blocks`, yields only `heading`/`paragraph`/`bullet` — the Phase A invariant holds on real flattened input
- [x] A sentence containing a literal `|` survives unflattened
- [x] `ats._clean_description` is imported, not reimplemented (assert by grepping the module for a second HTML stripper)
- [x] No placeholder/TODO comments in new code
- [x] detect-stack: no stack markers for this project — verification is plan-declared only

### [x] Phase D: the OOXML writer
- [x] Write failing test for `docx.render("# Ali\n", path)` producing a file whose `zipfile.ZipFile(path).namelist()` equals the five parts listed in "The five OOXML parts" above. Expected error: `AttributeError: module 'docx' has no attribute 'render'`
- [x] Run tests, confirm it fails for that reason
- [x] Implement `render(markdown, path)`: run `ats_lint` → raise `UnverifiedClaimError` on any `unverified-claim` finding (unless `allow_unverified=True`), then `flatten`, then `parse_blocks`, then emit the five parts with `zipfile.ZIP_DEFLATED`. Use the style table and the escaping order pinned above
- [x] Write to a temp file in the destination directory and `os.replace` it into place, so a failure never leaves a half-written `.docx` — the same atomic-write pattern `scripts/jobq.py::update_rows` already uses
- [x] Add tests for the enumerated edge cases: empty markdown (**refuse** — `EmptyDocumentError`; an empty CV is never the intent), whitespace-only markdown (same), a single heading and nothing else, 500 blocks, text containing `&`/`<`/`>` (assert the escaping order by round-tripping `a & b < c`), non-ASCII (`—`, `é`, `日本語`), a destination directory that does not exist, and a destination that is not writable
- [x] Round-trip test: write a `.docx`, reopen it with `zipfile`, assert `testzip() is None` and that `word/document.xml` parses under `xml.etree.ElementTree` — a well-formedness check the writer cannot fake
- [x] Run tests, confirm all pass
- [x] Commit: "feat(docx): write the OOXML parts, atomically"
- [x] `python3 -m compileall -q scripts tests` passes
- [x] `python3 -m unittest discover -s tests -t .` passes
- [x] Generated `.docx` is a valid ZIP (`testzip() is None`) and `word/document.xml` is well-formed XML
- [x] `a & b < c` round-trips exactly — proves the escaping order
- [x] A failed render leaves no file at the destination path
- [x] Empty and whitespace-only markdown both raise `EmptyDocumentError`
- [x] No placeholder/TODO comments in new code
- [x] detect-stack: no stack markers for this project — verification is plan-declared only

### [x] Phase E: CLI subcommand and skill wiring
- [x] Write failing test for `main(["render-docx", "--in", md_path, "--out", docx_path])` exiting 0 and printing JSON carrying `{"out": ..., "blocks": <int>, "notes": [...]}`. Expected error: `SystemExit: 1` with `{"error": "UsageError", "message": "argument command: invalid choice: 'render-docx'"}`
- [x] Run tests, confirm it fails for that reason
- [x] Add `cmd_render_docx` and its subparser: `--in` (required), `--out` (required), `--allow-unverified` (`store_true`, help text stating it is an opt-in escape from a safety gate and never a config default)
- [x] Emit `notes` on stdout as part of the JSON **and** on stderr as human lines — stdout is the parsed channel, stderr is the one a person reads
- [x] Extend `skills/tailor/SKILL.md`: the command block, what a refusal looks like, and the rule that `--allow-unverified` is Ali's decision per run, never the skill's
- [x] Update `CLAUDE.md`: add `render-docx` to the subcommand list and `scripts/docx.py` to the Layout table
- [x] Add tests: `render-docx` on markdown containing `[verifikasi]` returns exit 1 with `{"error": "UnverifiedClaimError"}` and **writes no file** (assert `os.path.exists(out) is False`); the same input with `--allow-unverified` exits 0 and writes the file; a missing `--in` is a `UsageError`
- [x] Run tests, confirm all pass
- [x] Commit: "feat(cli): render-docx, wired into tailor"
- [x] `python3 -m compileall -q scripts tests` passes
- [x] `python3 -m unittest discover -s tests -t .` passes
- [x] `tests/test_manifest.py` collects `render-docx` and all three of its flags — run `_documented_commands()` and assert they appear, since that guard was twice found vacuous in AJOB-1
- [x] Mutation check: document `--alow-unverified` in `skills/tailor/SKILL.md`, confirm the manifest test FAILS, revert
- [x] A refused render leaves no file on disk, asserted not assumed
- [x] `CLAUDE.md` lists `render-docx` and `scripts/docx.py`
- [x] No placeholder/TODO comments in new code
- [x] detect-stack: no stack markers for this project — verification is plan-declared only

### [x] Phase F: does it actually open?
- [x] Write failing test for the presence of `docs/evals/docx-rendering.md` and its declaring at least one case per target application. Expected error: `AssertionError: missing docs/evals/docx-rendering.md`
- [x] Run tests, confirm it fails for that reason
- [x] Write `docs/evals/docx-rendering.md` with one case each for **Microsoft Word**, **Google Docs** and **LibreOffice**: open the generated file, confirm headings render as headings, bullets as bullets, and that no markdown syntax is visible. Each case names the exact file to open and what a pass looks like
- [x] Generate a sample `.docx` from a real tailored CV into `docs/evals/samples/` and commit it, so the manual check has a fixed artifact rather than one the checker must first produce
- [x] Run tests, confirm all pass
- [x] **Hand the manual check to the owner.** Do NOT tick it. Record in the ledger under `## Utang terbuka`: `Word / Google Docs / LibreOffice open-check: NOT RUN — needs Ali, cannot be verified from the session`
- [x] Commit: "test(docx): eval cases for the one thing a program cannot check"
- [x] `python3 -m compileall -q scripts tests` passes
- [x] `python3 -m unittest discover -s tests -t .` passes
- [x] `docs/evals/docx-rendering.md` exists and names all three applications
- [x] A sample `.docx` is committed under `docs/evals/samples/`
- [x] The manual open-check is recorded as **NOT RUN** in the ledger, not ticked
- [x] No placeholder/TODO comments in new code
- [x] detect-stack: no stack markers for this project — verification is plan-declared only

## Phase log

| Phase | Status | Commit |
|---|---|---|
| A — parse blok markdown | SELESAI | `5c3ecfa` |
| B — gerbang klaim tak terverifikasi | SELESAI | `e201085` |
| C — perataan ATS | SELESAI | `1021366` |
| D — penulis OOXML | SELESAI | `6b6f620` |
| E — subcommand CLI + wiring skill | SELESAI | `b858ab7` |
| F — apakah benar-benar bisa dibuka | SELESAI | `c2d202d` |

## Utang terbuka

- ~~Perbaikan 11 konstruksi markdown (spec §5)~~ — **SELESAI** `c161292`, termasuk Minor bullet yatim. Tidak ada lagi sintaks markdown yang bocor ke dokumen dari kesebelas konstruksi itu.

- Aturan berhenti audit: **TERJAWAB OLEH PERISTIWA, bukan oleh keputusan.** Ali bilang 'merge sekarang', yang memanggil `gaspol-finish`, yang Step 2-nya WAJIB plan-verifier — jadi ronde 6 tetap jalan, hanya dibatasi ke diff sesudah ronde 5 (`2e7e8a6..HEAD`) sesuai aturan fix-loop. Hasilnya di `## Log`: BLOCKING lagi, 2 DIVERGED + 3 PARTIAL, dan dua di antaranya kehilangan isi diam-diam yang DIBAWA MASUK oleh commit perbaikan 11 konstruksi itu sendiri. Datanya sekarang: enam ronde, enam BLOCKING. Pilihan (a)/(b)/(c) di bawah masih milik Ali untuk ronde 7 dan seterusnya. Data ronde 5: menemukan jenis KETIGA lagi (kehilangan isi di jalur perbaikan), dan menemukannya karena diarahkan ke permukaan yang belum diaudit — pola yang sama dengan ronde 4. Tapi ronde 5 juga mencatat bahwa C-1/M-2/M-3/M-4 semuanya SATU akar bentuk: transformasi yang jalan di atas teks yang sudah diperiksa guard, atau pola yang lebih sempit dari markdown yang orang benar-benar tulis. Itu kelas yang bisa dihabiskan, bukan ekor tak berujung. Ronde 5 sudah selesai dan hasilnya ada di `## Log`. Tiga pilihan yang diajukan ke Ali: (a) berhenti begitu satu ronde nol Critical dan nol jenis baru — usulan Claude; (b) berhenti sesudah ronde 5 apa pun hasilnya, sisa temuan Minor dicatat jadi batas di bagian ini, bukan diperbaiki; (c) terus sampai bersih, tanpa batas, ~130-170k token per ronde.

- Word / Google Docs / LibreOffice open-check: **NOT RUN** — butuh Ali, tidak bisa diverifikasi dari dalam sesi. Phase F menulis kasus eval-nya; centangnya bukan milik Claude.

## Log
- 2026-09-19 plan ditulis — NEXT: Phase A
- 2026-09-19 Phase A selesai — `scripts/docx.py::parse_blocks` + 26 test baru, 288 lulus / 0 gagal. Cacat ditemukan saat dijalankan pada CV nyata: bullet yang terbungkus ke baris berikut pecah jadi paragraf liar; baris berindentasi sekarang menyambung bullet di atasnya. Commit `feat(docx): parse the supported markdown subset into blocks`. — NEXT: Phase B
- 2026-09-19 Phase B selesai — `ats_lint` + `unverified_findings` + `UnverifiedClaimError`, 318 lulus / 0 gagal. Mutation check: buang separuh `[Assumption]` → 1 test gagal; buang `re.I` → 3 gagal; buang syarat baris pemisah tabel → MASIH HIJAU pada percobaan pertama karena test pipa-literal cuma satu baris, dan satu baris tak pernah bisa jadi tabel. Ditambal 3 kasus multi-baris, mutasi yang sama sekarang gagal ketiganya. Commit `feat(docx): refuse to render a claim the candidate never verified`. — NEXT: Phase C
- 2026-09-19 Phase C selesai — `flatten` + `find_tables` + `strip_html`, 355 lulus / 0 gagal. Dua cacat baru muncul saat dijalankan pada CV berantakan nyata: (1) `ats._TAG_RE` = `<[^>]+>` memakan tengah kalimat "p95 < 200ms dan > 1k rps" — kurung sudut di luar tag asli sekarang disembunyikan dulu di balik sentinel, `ats._clean_description` tetap yang menghapus tag; (2) tabel satu kolom tidak terdeteksi karena pola pemisah menuntut minimal dua sel. Mutation check: buang proteksi non-tag / buang pad "N/A" / buang padding baris pendek — ketiganya gagal test. Commit `feat(docx): flatten what an ATS parses badly, refuse nothing`. — NEXT: Phase D
- 2026-09-19 Phase D selesai — `render` + lima bagian OOXML + tulis atomik, 385 lulus / 0 gagal. Dua cacat ketemu, dua-duanya dari menjalankan bukan membaca: (1) `tempfile.mkstemp` di luar guard, folder tak bisa ditulis melempar `PermissionError` telanjang — sekarang `DestinationError`; (2) file hasil dibaca balik lewat extractor dokumen nyata menunjukkan tabel skill 3 baris jadi SATU kalimat panjang — baris tabel sekarang jadi bullet, tiap baris satu blok. Bullet pakai glyph harfiah, bukan numbering.xml. Timestamp zip dikunci supaya byte hasil render sama tiap kali. Commit `feat(docx): write the OOXML parts, atomically`. — NEXT: Phase E
- 2026-09-19 Phase E selesai — subcommand `render-docx` + wiring `skills/tailor/SKILL.md` + `CLAUDE.md`, 402 lulus / 0 gagal. `--out` sama dengan `--in` ditolak (dibanding lewat path absolut) supaya .docx tidak menimpa markdown sumbernya. Mutation check manifest: tulis `--alow-unverified` di SKILL.md → test flag gagal; salah eja nama subcommand → test subcommand gagal. Ditambah test eksplisit bahwa guard benar-benar mengumpulkan `render-docx` dan ketiga flag-nya — guard ini dua kali ketahuan hampa di AJOB-1. Commit `feat(cli): render-docx, wired into tailor`. — NEXT: Phase F
- 2026-09-19 Phase F selesai — `docs/evals/docx-rendering.md` (kasus Word / Google Docs / LibreOffice) + sampel tetap `docs/evals/samples/tailored-cv-sample.docx`, 409 lulus / 0 gagal. Sampel di-commit, bukan dibuat saat dibutuhkan, dan ada test yang membuat ulang lalu membandingkan byte per byte — render reproducible karena timestamp zip dikunci. Cek buka-file manual TIDAK dicentang: tetap utang terbuka milik Ali. Commit `test(docx): eval cases for the one thing a program cannot check`. — NEXT: verifikasi akhir (gaspol-verify + plan-verifier)
- 2026-09-19 Verifikasi akhir — plan-verifier: MISSING 0, DROPPED 0, placeholder 0, lima bagian OOXML byte-identik dengan plan. code-review menemukan 3 Critical, ketiganya saya reproduksi sendiri lewat CLI sebelum diperbaiki: (1) `[**verifikasi**]` / ``[`verifikasi`]`` / `&#91;verifikasi&#93;` lolos gerbang karena `strip_inline` dan pembersih html jalan SESUDAH pengecekan — penanda merakit diri sendiri di hilir, file tetap ditulis, dan kata "[verifikasi]" tercetak di dokumen yang dikirim; (2) tidak ada pengecekan ulang pada teks yang benar-benar ditulis; (3) `--in cv.md --out CV.MD` MENGHANCURKAN markdown sumber di macOS (APFS tidak peka huruf besar-kecil) — perbandingan string `abspath` melihat dua file padahal di disk satu. Perbaikan: `_unmask()` (entity di-decode sampai titik tetap, tag dibuang tanpa pemisah, markup inline dilucuti) + lapis kedua pada teks hasil render + `os.path.samefile`. Pola penanda diperlebar ke `[Assumption: <alasan>]`. Tiga guard ternyata hijau tapi hampa (dibuktikan dengan mutasi): test sisa temp-file, test "NOT RUN" di ledger, dan test sintaks markdown yang dijalankan pada fixture tanpa `](` maupun `|---`. 427 lulus / 0 gagal. Commit `1d6e166`, `19e43cf`. — NEXT: keputusan Ali soal teks plan/spec yang sudah tidak cocok dengan kode
- 2026-09-19 Keputusan Ali diambil (4 pertanyaan): samakan plan+spec ke kode; tiap sel tabel bawa header sendiri; `--allow-unverified` buang penanda tapi klaim tetap; `--out` wajib `.docx`. Dikerjakan di `7932b4a` (kode) + `587c68d` (plan/spec) + `6231cc1`, `02d3b71` (SKILL.md). — NEXT: probe balik gerbangnya
- 2026-09-19 Probe mandiri 15 ejaan penanda pada gerbang yang baru diperbaiki — 3 lolos: `[__verifikasi__]`, `[_verifikasi_]`, dan zero-width di tengah kata. Ditutup di `cecf0c2`; emphasis garis bawah ikut dilucuti, dan emphasis hanya dilucuti DI LUAR code span supaya `` `__init__` `` tidak jadi `init`. Homoglyph Cyrillic sengaja dibiarkan dan dipatok test sebagai batas yang tercatat. 441 lulus. — NEXT: plan-verifier ronde 2
- 2026-09-19 plan-verifier ronde 2 — MISSING 0, DROPPED 0, placeholder 0, lima bagian OOXML tetap byte-identik, ketiga guard yang dulu hampa sekarang bergigi. TAPI menemukan bypass KEEMPAT dan saya reproduksi sendiri: karakter kontrol C0 (`\x01`, `\x02`, `\x1f`, `\x7f`) dan soft hyphen di tengah kata menembus KEDUA lapis gerbang, lalu `escape()` — yang jalan SESUDAH pengecekan terakhir — menghapus karakter itu dan merakit ulang `[verifikasi]` utuh di dokumen yang dikirim. Sebabnya struktural: himpunan karakter yang dinormalkan gerbang beda dari yang dihapus penulis XML. Diperbaiki di `db7f0d3`: `_ILLEGAL_XML_RE` sekarang subset `_INVISIBLE_RE` secara konstruksi, dipatok test yang menyapu setiap codepoint di bawah 0x11000. Proyeksi juga NFKC, jadi `［verifikasi］` fullwidth ikut tertangkap. `_strip_markers` punya bug cerminnya (penanda ejaan entity tidak terhapus saat `--allow-unverified`, `notes` lapor 0) — sekarang memakai proyeksi yang sama dengan gerbang. 446 lulus / 0 gagal. — NEXT: sapu gerbang secara sistematis
- 2026-09-19 Sapuan sistematis gerbang — bukan lagi tebak-tebakan ejaan. Setiap codepoint berkategori Unicode `Cc`/`Cf` dicoba disisipkan di tengah kata penanda: **183 lolos**, semuanya tak terlihat di halaman (kontrol C1, blok tag, format musik). Daftar rentang yang ditulis tangan memang selalu ketinggalan. Definisi diganti ke kategori Unicode itu sendiri di `062471e`, disertai test yang menyapu ulang setiap codepoint. Sisa 8 karakter adalah pemisah baris bagi `str.splitlines` — tidak mungkin ditangkap per baris karena penanda sudah terbelah sebelum ada baris; `render` sekarang menyambung blok tanpa pemisah juga, dan kedelapannya menolak. Sapuan ulang tingkat render: 110 codepoint, **0 bocor**. Biaya diukur: import 0,030 detik, lint 50 salinan CV berantakan 0,004 detik. 450 lulus / 0 gagal. — NEXT: serangan mandiri sambil menunggu audit ronde 3
- 2026-09-19 Serangan mandiri 13 kombinasi — **2 bocor, dua-duanya saya temukan sendiri**: penanda yang terbelah spasi di TENGAH kata lolos. `- ARR [veri\n  fikasi]` disambung aturan continuation jadi `[veri fikasi]`, dan `[veri<span>  </span>fikasi]` diciutkan pembersih html jadi hal yang sama — keduanya sesudah gerbang membaca barisnya. Ditutup dengan `_UNVERIFIED_LOOSE_RE` yang menoleransi spasi ANTAR HURUF, dipakai hanya pada proyeksi. Sengaja TIDAK ada `\\s*` sesudah kurung buka supaya `[ Assumption ]` tetap bukan kecocokan — itu kontrak yang dipatok plan Phase B, dan spasi di depan itu penulis menulis hal lain, beda dari spasi di tengah kata yang berarti ada transformasi yang membelahnya. Empat kasus prosa berkurung biasa diuji tidak ikut kena. 455 lulus / 0 gagal. — NEXT: audit ronde 3
- 2026-09-19 plan-verifier ronde 3 — verdict BLOCKING, dan benar. Definisi 'tak terlihat' saya (`Cc`/`Cf`) TERLALU SEMPIT: **267 codepoint bernama** masih menulis `[verifikasi]` yang terbaca ke dalam dokumen, tanpa `--allow-unverified`. Semua variation selector (`U+FE00`-`U+FE0F`, `U+E0100`) berkategori `Mn`, bukan `Cf`. Hangul filler `Lo`. Saya reproduksi 8 contoh lewat CLI sebelum memperbaiki. Lebih buruk: test sapuan saya mengulang predikat implementasinya sendiri, jadi TIDAK MUNGKIN menangkap ini — mempersempit kode bikin test merah, memperlebar kode tidak terlihat sama sekali. Dan saya menulis klaim 'variation selector sudah tertangani' di TIGA tempat, padahal salah. Diperbaiki: kategori jadi `Cc`/`Cf`/`Cn`/`Mn` + empat Hangul filler; test sapuan sekarang memakai daftar rentang `Default_Ignorable_Code_Point` dari UCD sebagai oracle independen — dipersempit balik ke `Cc`/`Cf`, 19 test merah. Klaim palsu dihapus dari ketiga tempat. Sekalian: plan bilang 'Seven points' padahal sembilan (dihitung, bukan ditebak), amendment 9 menyebut `_INVISIBLE_RE` yang sudah dihapus, dan padding baris tabel pendek ternyata kode mati. 458 lulus / 0 gagal. — NEXT: audit ronde 4
- 2026-09-19 plan-verifier ronde 4 — BLOCKING lagi, 2 Critical + 3 Major, semuanya saya reproduksi sendiri dulu. (4-E) `_ILLEGAL_XML_RE` daftar tangan, jadi U+FFFF di teks CV BIASA menghasilkan `.docx` yang tidak bisa dibuka parser mana pun sementara CLI lapor sukses exit 0 — sekarang komplemen produksi `Char` XML 1.0. Akar: test subset hanya menjaga arah GERBANG (`_ILLEGAL_XML_RE ⊆ _remove_invisible`); keabsahan XML butuh arah sebaliknya dan tidak ada yang mematoknya. (4-B) NFKC tidak cuma menguraikan, tapi MENYUSUN: `i`+U+0301 jadi `í`, Mn-nya hilang sebelum sempat dibuang — 17 combining mark menyembunyikan penanda. Mark sekarang dibuang sebelum DAN sesudah NFKC. (4-C) kategori `Me` + `Cs` ditambahkan. (4-A) 17 karakter spasi Unicode dan — ini yang paling penting — jalur ASCII MURNI: baris ter-wrap tepat sesudah `[` menghasilkan `[ Assumption: FY24 baseline]` di dokumen, exit 0, tanpa karakter aneh sama sekali. Ali memutuskan izinkan spasi sesudah `[` (diukur dulu: nol false positive baru pada korpus 12 baris); plan Phase B + checklist diamandemen, 3 assertion dibalik. (4-G/4-H) dua invariant yang disebut 'disengaja' di docstring tapi tidak dijaga test: tag dibuang tanpa pemisah, dan padding header yang mencegah `zip` memotong baris berlebih sel. Dua-duanya dipatok. (4-D) komentar lama masih membangun invariant di atas `_INVISIBLE_RE` yang sudah dihapus, dan 'by construction' itu overclaim. 466 lulus / 0 gagal. — NEXT: sapuan verifikasi sesudah perbaikan ronde 4
- 2026-09-19 Sapuan verifikasi sesudah semua perbaikan ronde 4 — **tingkat render, bukan tingkat lint**: 8.122 codepoint (seluruh `Cc`/`Cf`/`Cn`/`Cs`/`Me`/`Mn`/`Zs`/`Zl`/`Zp` di bawah 0x11000 plus seluruh himpunan Default_Ignorable termasuk blok Egyptian Hieroglyph Format Controls U+13430-U+1343F yang tadinya hilang dari oracle test) disisipkan di tengah kata penanda lalu dokumennya dibaca balik: **nol bocor**. Keabsahan XML disampel 9.948 codepoint: **nol dokumen tak terbaca**. Lima bagian OOXML tetap byte-identik dengan plan, sampel eval tetap reproducible (`f2a998ac04a4d8fe`). 466 lulus / 0 gagal, 29 commit, pohon bersih. — NEXT: ronde 5
- 2026-09-19 plan-verifier ronde 5 **DIKIRIM, HASIL BELUM ADA**. Sengaja TIDAK disuruh mengulang sapuan gerbang (sudah jenuh, lihat baris di atas). Diarahkan ke permukaan yang belum pernah diaudit sama sekali: (1) kehilangan/korupsi data diam-diam di `flatten` dan `parse_blocks` pada CV biasa — tabel dengan titik dua perataan, pipa ter-escape, URL berkurung, daftar bernomor, setext heading, blockquote, code fence, entity di prosa normal; (2) keabsahan OOXML di luar well-formed — urutan elemen `word/styles.xml` menurut skema wordprocessingml, style id yang dirujuk tapi tidak didefinisikan, posisi `docDefaults` (file yang Word buka setelah 'repair' diam-diam tetap cacat); (3) kontrak CLI: satu dokumen JSON di stdout, log di stderr, refusal `{"error","message"}` exit 1, tidak pernah traceback — dicoba dijebol lewat FIFO, `/dev/null`, path panjang, run bersamaan ke `--out` yang sama, filesystem read-only; (4) atomicity: symlink di `--out`, loop symlink, direktori hilang di tengah jalan; (5) apakah guard di `tests/test_evals.py` dan `tests/test_manifest.py` hampa; (6) `cmd_render_docx` dan jalur AJOB-1 yang ticket ini sentuh. — NEXT: baca hasil ronde 5
- 2026-09-19 plan-verifier ronde 5 — **BLOCKING**. 1 Critical, 6 Major, 8 Minor. Semua saya reproduksi sendiri dulu. Ronde ini sengaja TIDAK menyentuh gerbang (sudah jenuh) dan diarahkan ke permukaan yang belum pernah diaudit — hasilnya menemukan **jenis ketiga**: kehilangan isi diam-diam di jalur perbaikan, bukan gerbang, bukan penulis XML.
  - **C-1 (Critical, SUDAH DIPERBAIKI `3c0e129`)** — entity di sebelah tag apa pun MENGHAPUS angka milik kandidat. `ats._clean_description` meng-unescape sampai titik tetap LALU baru membuang tag, jadi `&lt;` jadi `<` mentah SESUDAH `_protect_non_tags` jalan, dan `<[^>]+>` memakan sisa kalimat. `<b>Budget</b>: kept spend &lt; $2M while headcount &gt; 40.` sampai ke halaman sebagai `Budget: kept spend 40.` — CV menyatakan sesuatu yang penulisnya tidak pernah tulis, exit 0, catatan stderr cuma menyebut tag `<b>`. Bentuknya sama persis dengan bug yang docstring `_unmask` ditulis untuknya, satu lapis lebih bawah. Perbaikan: entity di-decode SEBELUM sentinel dipasang.
  - **M-6 (SUDAH DIPERBAIKI `3c0e129`)** — entity di prosa biasa tidak pernah di-decode. Apakah `AT&amp;T` tercetak benar tergantung apakah ada `<b>` nyasar di baris yang sama. Sekarang di-decode di semua baris, dan dilaporkan.
  - **M-1 (SUDAH DIPERBAIKI `3c0e129`)** — `w:pgMar` cuma punya 4 dari 7 atribut wajib `CT_PageMar`, jadi SETIAP `word/document.xml` gagal validasi ISO/IEC 29500 — termasuk sampel eval yang di-commit, yaitu artefak yang justru dipakai untuk cek buka-file manual. File yang Word buka sesudah 'repair' diam-diam tetap cacat. Blok XML yang dipatok plan ikut diubah (Amendment 11) dan sampel di-regenerate.
  - **Item yang BERSIH (3 dari 6)** — kontrak CLI tidak bisa dijebol (FIFO, `/dev/null`, path >255 byte, filesystem read-only, run bersamaan: semua `{"error","message"}` exit 1, nol traceback, nol polusi stdout); atomicity tidak bisa dijebol (symlink di `--out` mengganti symlink-nya bukan targetnya; symlink ke file sumber ditolak `_same_file`; 12 render bersamaan ke satu `--out` menghasilkan arsip 5 bagian yang valid, nol temp tertinggal); guard di `test_evals.py` dan `test_manifest.py` TIDAK hampa — 13 mutasi, semuanya merah.
- 2026-09-19 **Pelanggaran spec §5 — 11 konstruksi markdown diubah/dirusak TANPA satu baris pun di stderr.** Spec §5 berbunyi: 'Everything else is flattened or dropped **with a line on stderr**'. `skills/tailor/SKILL.md` mengulangi janji itu dan menyuruh model melaporkan apa yang berubah — lalu diberi daftar kosong. Empat merusak CV biasa: daftar bernomor jadi satu paragraf panjang dengan nomornya bocor; tabel dengan pemisah GFM satu strip (`| :-: |`) tidak dikenali sama sekali lalu seluruh tabel jadi satu paragraf berisi pipa mentah; pipa ter-escape memecah sel dan menyuntik ` — ` ke dalam kalimat kandidat; setext heading menempelkan `===========` di belakang nama kandidat. Tujuh sisanya membocorkan sintaks markdown mentah (blockquote `>`, backtick code fence, `[x]` task list, definisi footnote/reference, link dengan kurung di teksnya) — itu juga melanggar kriteria eval Phase F 'no markdown syntax is visible'.
- 2026-09-19 Ali memutuskan: **perbaiki semua 11**, bukan sebagian. — NEXT: kerjakan 11 konstruksi itu
- 2026-09-19 Progres perbaikan 11 konstruksi: **M-2 dan M-3 SELESAI** (belum di-commit saat baris ini ditulis). `_TABLE_SEP_RE` sekarang menerima satu strip sesuai GFM, bukan tiga — syarat 'harus ada pipa di baris pemisah' dipertahankan supaya `---` horizontal rule dan setext underline tidak ikut terbaca sebagai tabel. `_split_row` memakai `(?<!\\)\|` jadi pipa ter-escape tetap jadi isi sel, bukan pemisah. 471 lulus / 0 gagal. — NEXT: M-4, M-5, dan 7 konstruksi sisanya
- 2026-09-19 **Ketahuan: M-2 dan M-3 ter-commit TANPA test.** Saya mutasi balik keduanya — **0 test merah**. Itu persis pola cacat yang CLAUDE.md repo ini peringatkan, dan saya yang melakukannya. Ketahuan juga bahwa keduanya tersapu `git add -A` ke dalam commit berlabel `chore(ledger)` (`6d88315`), jadi label commit-nya menyesatkan: ada perubahan `scripts/docx.py` di dalam commit ledger. Test ditambahkan; mutasi yang sama sekarang 4 dan 2 merah.
- 2026-09-19 **11 konstruksi SELESAI** (`c161292`). Yang merusak isi: daftar bernomor jadi satu blok per item dengan nomornya dipertahankan dan tanpa glyph; setext `===` dan `---` jadi heading level 1 dan 2 (rule tanpa teks di atasnya tetap rule); pemisah tabel GFM satu strip; pipa ter-escape tetap isi sel. Yang membocorkan sintaks: blockquote, backtick code fence, indentasi 4 spasi, checkbox task list, definisi footnote dan reference, serta kurung di teks link. Baris kode dipisah jadi blok sendiri supaya tidak tergabung jadi `def solve(x): return x`. Link reference sekarang DIRESOLVE dari definisinya, bukan dilewatkan begitu saja, dan bilang kalau definisinya tidak ada. Plus: bullet yang isinya cuma penanda tidak lagi jadi `• ` yatim saat `--allow-unverified`, dan cek dokumen kosong dipindah ke SESUDAH penghapusan penanda supaya dokumen berisi penanda saja menolak, bukan menulis halaman kosong. Indentasi di bawah list item tetap dibaca sebagai bullet terbungkus, bukan kode. Sembilan handler baru dimutasi satu per satu: 1/2/1/3/3/1/1/2/2 test merah. 492 lulus / 0 gagal. — NEXT: keputusan Ali soal ronde 6, cek buka-file manual, dan merge
- 2026-09-19 Ali bertanya berapa ronde audit yang harus dijalani. Jawaban jujur: **gaspol-auto tidak mematok angka**. Data: ronde 1 tiga Critical, ronde 2 satu, ronde 3 satu, ronde 4 dua Critical + tiga Major. Tingkat temuan BELUM turun. Tapi ronde 4 menemukan jenis baru (`.docx` rusak dilaporkan sukses — bukan soal gerbang sama sekali) karena diarahkan ke permukaan lain, bukan karena gerbangnya masih bocor. Usul aturan berhenti: **berhenti begitu satu ronde nol Critical DAN nol jenis baru**. Keputusan ini masih terbuka — lihat `## Utang terbuka`.
- 2026-09-19 Ali bilang **'merge sekarang'**. Itu memanggil `gaspol-finish`, yang Step 2-nya BLOCKING dan wajib — jadi ronde 6 tetap jalan, dibatasi ke `2e7e8a6..HEAD` (5 commit) sesuai aturan fix-loop, bukan mengulang seluruh branch. Step 1 lewat lebih dulu: 492 lulus, `compileall` bersih, worktree bersih, `CLAUDE.md` terbukti ada di diff branch. Dicatat juga: repo ini **tidak punya remote** (`git remote -v` kosong), jadi opsi push/PR tidak ada — merge lokal saja.
- 2026-09-19 plan-verifier ronde 6 — **BLOCKING**. 2 DIVERGED, 3 PARTIAL, 2 Minor, MISSING 0, placeholder 0. Yang penting: **dua cacat terparah DIBAWA MASUK oleh `c161292` sendiri**, commit yang seharusnya menutup pelanggaran spec §5. Saya reproduksi dua-duanya lewat `flatten` + `parse_blocks` sebelum menyentuh apa pun.
  - **Amendment 2 kambuh, satu baris lebih dalam.** Bullet yang terbungkus ke baris KETIGA pecah jadi bullet + paragraf nyatim. Sebabnya `last_was_list_item` di-reset oleh baris lanjutan itu sendiri, jadi lanjutan berikutnya keluar dari konteks list dan dibaca sebagai indented code. Persis cacat yang Amendment 2 ada untuk mencegah, ditemukan lagi karena handler indented-code baru menabraknya.
  - **Emphasis dikupas DI DALAM fenced code.** Delimiter fence dibuang, isinya kehilangan proteksi code-span, lalu `strip_inline` jalan: `total = a*b*c` sampai ke halaman sebagai `total = abc`, `__init__` jadi `init`. Nol notes. Bentuk yang sama dengan C-1: transformasi yang jalan di atas teks yang sudah lolos pemeriksaan. Ironisnya plan:186-190 memang menulis janji sebaliknya, dan janji itu cuma dipegang untuk code span inline.
  - **Tiga konstruksi masih diam** — daftar bernomor, setext heading, task checkbox. Ketiganya ditransformasi di `parse_blocks`, yang tidak punya `notes`, jadi klaim ledger '11 konstruksi SELESAI' itu PARTIAL: penanganannya selesai, pelaporannya baru 7 dari 11. Komentar di test saya sendiri (`test_every_construct_that_changes_is_reported`) mengakui pengecualian itu secara eksplisit — pengecualian yang tidak ada di spec dan tidak pernah dicatat sebagai amendment. Test yang menjelaskan kenapa ia tidak menguji sesuatu bukan test.
  - Dua Minor: cabang notes basi yang bilang reference link 'left as written — out of scope' padahal sudah diresolve (satu input mengeluarkan DUA notes yang saling bertentangan), dan docstring `flatten` yang menjanjikan nomor baris asli sementara `_flatten_blocks` membuang/menyisipkan baris sehingga tag di baris 3 dilaporkan 'line 2'.
- 2026-09-19 **Perbaikan ronde 6 selesai.** Ditulis test dulu: 15 test baru, 10 merah sebelum kode disentuh. Fenced dan indented code sekarang dibungkus jadi code span (delimiter satu backtick lebih panjang dari run terpanjang di dalamnya, dengan bantalan spasi kalau barisnya sendiri diawali/diakhiri backtick); `_CODE_RE` dilebarkan ke span multi-backtick, yang memang yang CommonMark tetapkan. Baris berindentasi di bawah list item mempertahankan konteks list. Daftar bernomor, setext underline, dan task checkbox masing-masing dapat baris stderr — setext hanya kalau ada paragraf terbuka di atasnya, supaya horizontal rule dan pemisah tabel tidak ikut diumumkan. Baris berjalan sebagai pasangan `(nomor_baris_asli, teks)` sejak block pass, jadi janji docstring-nya sekarang benar. Cabang notes basi dihapus. Amendment 12 dan 13 dicatat di plan.
- 2026-09-19 **Sapuan gerbang diulang penuh, karena `_CODE_RE` dipakai `_unmask`.** Melebarkan code span menyentuh proyeksi yang dipakai gerbang klaim-belum-terverifikasi, jadi tidak boleh diasumsikan aman. Disapu **1.112.064 codepoint** (seluruh Unicode minus surrogate) disisipkan di tengah kata penanda, lalu `.docx`-nya dibuka dan teksnya dibaca balik: **nol bocor, nol dokumen tak terbaca**. Sapuan pertama saya sendiri SALAH dan saya buang — template-nya menguji `[verifikasi0]` dan `[!Assumption]`, yang secara kontrak memang bukan penanda, lalu melaporkan 290.188 'bocor' palsu.
- 2026-09-19 **13 mutasi, 13 merah** — tapi tidak pada percobaan pertama. Putaran pertama: 3 guard lolos hidup-hidup (bantalan code span, nomor baris asli di note tabel, nomor baris di note reference link). Bantalannya luput karena di tepi blok `.strip()` menutupi kegagalannya; baru kelihatan dengan code span di TENGAH kalimat. Tiga test ditambahkan, mutasi diulang, ketiganya merah. Dua test lama ikut diperketat, bukan dilonggarkan: `test_fenced_code_keeps_its_lines_apart` sekarang memeriksa teks dokumen jadinya (backtick perantara memang wajar), dan `test_a_horizontal_rule_is_still_not_a_table_separator` sekarang mematok note setext-nya alih-alih menuntut notes kosong. Sampel eval tetap byte-identik (`0957da11`). 512 lulus / 0 gagal.
- 2026-09-19 **gaspol-finish Step 2 ronde 7 — BLOCKING, satu Critical, dan penyebabnya perbaikan saya sendiri di ronde 6.** Melebarkan `_CODE_RE` ke span multi-backtick dan membungkus tiap baris kode jadi code span membuat **kedalaman sarang code span dikendalikan penulis**. `strip_inline` mengupas tepat satu lapis, dan gerbang menjalankannya dua kali (markdown mentah, lalu teks hasil render) — dua ronde satu lapis BUKAN dua lapis. ``` `[**verifikasi**]` ``` lolos keduanya dan tercetak di halaman. Saya reproduksi enam bentuk lewat `render()` lalu membaca `.docx`-nya balik: prosa biasa, bullet, di dalam fence, di tengah baris fence, indented code, dan `[**Assumption**: FY24]`. **Enam-enamnya SHIPS.** CV yang `317cd2a` tolak, `b50c449` kirim.
  - Kenapa sapuan 1.112.064 codepoint saya tidak bisa melihatnya: sapuan itu menyisipkan SATU codepoint di tengah kata. Bocor ini bukan soal karakter — teks penandanya ASCII utuh — melainkan soal KEDALAMAN SARANG. Generatornya tidak pernah bisa mengeluarkan lapisan backtick kedua, jadi seluruh kelas ini di luar alfabetnya. Docstring `_unmask` menulis bahwa transformasi apa pun di masa depan 'hanya bisa membuat penanda LEBIH terlihat' — itu benar untuk transformasi yang MENGHAPUS karakter, dan diam soal yang MENAMBAH delimiter pelindung. `_as_code_span` justru yang kedua.
  - Diperbaiki: `_unmask` mengupas `strip_inline` sampai titik tetap, trik yang sudah dipakainya untuk `html.unescape` dan dengan alasan yang sama. Klaim di docstring dikoreksi, bukan dihapus.
- 2026-09-19 **gaspol-finish Step 3 (gaspol-review Tier 1) — nol Critical, 3 Important, ketiganya saya reproduksi sendiri.** (1) Note daftar bernomor MENGATAKAN KEBALIKAN dari yang kodenya lakukan: tertulis 'rendered as a bullet, numbering dropped', padahal nomornya dipertahankan dan hasilnya paragraf, bukan bullet — dan 40 item jadi 40 baris stderr. Lolos karena test saya sendiri cuma cek substring `'ordered list' in n`, yang tidak mungkin melihat kebohongan itu. (2) Item bernomor yang terbungkus pecah jadi paragraf yatim — setengah kalimat di halaman yang dibaca perekrut. (3) **Kode berindentasi sesudah daftar dirusak diam-diam**: `def __init__` jadi `def init`, `a*b*c` jadi `abc`, notes `[]`. Itu akibat langsung keputusan saya di ronde 6 untuk TIDAK me-reset konteks list pada baris kosong, dengan alasan 'risiko lebih rendah'. Keputusan itu salah.
- 2026-09-19 **Perbaikan ronde 7 selesai.** 16 test baru, 17 merah sebelum kode disentuh. Gerbang mengupas sampai titik tetap. Baris yang seluruhnya satu code span dilewatkan semua pass prosa — `List<String> parse(Vec<T> x)` dan `&#96;` sekarang harfiah. Escape backslash disembunyikan sebelum emphasis dilucuti lalu dikembalikan tanpa backslash-nya; ini membuat gerbang LEBIH ketat, karena `\[verifikasi\]` ikut tertangkap. Item bernomor punya sasaran continuation. Baris kosong mengakhiri list item. Note daftar bernomor jadi satu baris per RUN dan berbunyi benar; rewrite `)` jadi `.` dilaporkan tersendiri. Cabang blockquote melapor konstruksi yang sama dengan jalur lain.
- 2026-09-19 **Verifikasi ronde 7.** Sapuan sarang: 39.564 kombinasi (emphasis, code span multi-backtick, entity, tag, backslash sampai kedalaman 3, di 7 konteks: prosa, bullet, fence, indented code, blockquote, sel tabel, daftar bernomor) — **nol bocor**. Sapuan codepoint diulang karena `strip_inline` berubah lagi: 1.112.064 di bullet plus 821.820 masing-masing di dalam fence, code span, dan blockquote — **3.577.524 render, nol bocor, nol dokumen tak terbaca**. 10 mutasi guard baru, **10 merah**. Sampel eval tetap byte-identik (`0957da11`). 536 lulus / 0 gagal. Amendment 14 dan 15 dicatat di plan.
